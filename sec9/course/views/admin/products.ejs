<%- include('../includes/head') %>
<link rel="stylesheet" href="/css/product.css">
</head>

<body>
    <%- include('../includes/navigation') %>

    <main>
        <% if (products.length > 0) { %>
        <div class="grid">
            <% products.forEach(function(product) { %>
            <article class="card product-item">
                <header class="card__header">
                    <h1 class="product__title"><%= product.title %></h1>
                </header>
                <div class="card__image">
                    <img src="<%= product.imageUrl || 'https://kbimages1-a.akamaihd.net/185e7463-9792-4728-8403-d91a6352d549/1200/1200/False/the-da-vinci-code-1.jpg' %>"
                        alt="<%= product.title %>" />
                </div>
                <div class="card__content">
                    <h2 class="product__price">$<%= product.price || "19.99" %></h2>
                    <p class="product__description"><%= product.description || "An interesting read!" %></p>
                </div>
                <div class="card__actions">
                    <a href="/admin/edit-product/<%= product.id %>" class="btn">Edit</a>
                    <form action="/admin/delete-product" method="POST">
                        <input type="hidden" name="productId" value="<%= product.id %>" />
                        <button class="btn" type="submit">Delete</button>
                    </form>
                </div>
            </article>
            <% }) %>
        </div>
        <% } else { %>
        <h3>No Products</h3>
        <% } %>
    </main>
    <%- include('../includes/end') %>